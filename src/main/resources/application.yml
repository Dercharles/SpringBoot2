spring:
  profiles:
    active: dev
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/demoDB?useSSL=false
    username: root
    password: admin
  jpa:
    show-sql: true